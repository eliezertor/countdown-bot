export type MeetingRecording = {
  id: string;
  title: string;
  description: string;
  createdAt: string;
  updatedAt: string;
};

export type RecordingIndicatorProps = {
  isRecording: boolean;
};
